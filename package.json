{
  "private": true,
  "scripts": {
    "dev": "node build.mjs --watch --serve",
    "build": "node build.mjs --minify"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "jsmediatags": "^3.9.7",
    "music-tempo": "^1.0.3"
  }
}
